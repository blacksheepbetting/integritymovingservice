# Set only after the final production domain is approved.
PUBLIC_SITE_URL=https://www.example.com

# Keep false for every staging or preview deployment.
# Change to true only after written approval to launch and index the site.
PUBLIC_INDEXING_ALLOWED=false
